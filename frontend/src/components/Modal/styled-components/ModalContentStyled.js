import styled from 'styled-components'

const InfoTopJob = styled.figure``

const LogoCompany = styled.img``

const JobSummary = styled.figure``

const List = styled.ul``

const ListItem = styled.li``

const Description = styled.div``

const Button = styled.button``

const TitleJob = styled.h2

const LocationJob = styled.h3``

const Salary = styled.span``

export { Button, Description, JobSummary, InfoTopJob, LocationJob, LogoCompany, List, ListItem, Salary, TitleJob }
