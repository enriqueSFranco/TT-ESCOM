import styled from 'styled-components'
import { Link } from 'react-router-dom'

const CardUser = styled.div`
  outline: 1px solid #ccc;
`

const LinkToFullProfile = styled(Link)``

const CardProfile = styled.figure``

const Speciality = styled.span``

const Item = styled.li``

const List = styled.ul``

const ContentInfo = styled.figcaption

export { CardUser, CardProfile, ContentInfo, Item, List, LinkToFullProfile, Speciality }