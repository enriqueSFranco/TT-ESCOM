import React from "react"

import styles from "./Styles.module.css";

const Select = () => {
  return (
    <select className={styles.inputLabel}>
      <option value="value1">MNX</option>
    </select>
  )
};

export default Select;