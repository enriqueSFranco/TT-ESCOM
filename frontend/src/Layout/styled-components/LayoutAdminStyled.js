import styled from 'styled-components'

const Content = styled.section`
`

const NavList = styled.ul``

const Item = styled.li``

const Wrapper = styled.main`
  position: relative;
  top: 4rem;
  display: grid;
  grid-template-columns: 400px 1fr;
  padding: 1rem;
`

export { Content, NavList, Item, Wrapper }